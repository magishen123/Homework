fio = input('>>')
l_fio = fio.split()
work = input('>>')
l_work = work.split()
group = input('>>')
l_group = group.split(',')
all_l = []
all_l.append(l_group)
all_l.append(l_work)
all_l.append(l_fio)

print(all_l)
